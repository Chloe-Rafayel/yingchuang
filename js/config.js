/**
 * 前端 Agent 配置 — 与 config.example.js 相同，可提交仓库
 */
window.EDU_AGENT_CONFIG = {
    apiProvider: 'zhipu',
    apiModel: 'glm-4-flash',
    useProxy: true,
    proxyUrl: '/api/chat',
    apiKey: ''
};
